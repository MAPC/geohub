// Municipality shapes, as TopoJSON
const BOUNDARY_TOPOJSON = __BOUNDARY_TOPOJSON__;

// Normalized name -> {muniId, canonical, subregion}
const MAPC_LOOKUP = __MAPC_LOOKUP__;

// Sample dataset preloaded so the map renders immediately without an upload
const SAMPLE_DATA = __SAMPLE_DATA__;

// Outline of the whole MAPC region, drawn around the outside of all 101 municipalities
const REGION_OUTLINE_TOPOJSON = __REGION_OUTLINE__;
